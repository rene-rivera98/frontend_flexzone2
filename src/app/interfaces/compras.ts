export interface Compras {
  id_compra: string,
  id_usuario: string,
  total_compra: number,
  subtotal_compra: number,
  total_productos: number,
  CFDI: string,
  createdAt: string,
  updatedAt: string
}
